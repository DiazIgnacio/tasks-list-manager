{
  "version": "0.0.1",
  "builds": [
    {
      "src": "client/package.json",
      "use": "@vercel/node",
      "config": { "maxLambdaSize": "10mb" }
    },
    {
      "src": "server/package.json",
      "use": "@vercel/node",
      "config": { "maxLambdaSize": "10mb" }
    }
  ],
  "projects": [
    {
      "name": "client",
      "type": "static",
      "src": "client",
      "build": {
        "cmd": "npm run build",
        "output": "dist/client"
      }
    },
    {
      "name": "server",
      "type": "npm",
      "src": "server",
      "build": {
        "cmd": "npm run build",
        "output": "dist/server"
      }
    }
  ],
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "server/$1"
    },
    {
      "src": "/(.*)",
      "dest": "/client/$1"
    }
  ]
}
